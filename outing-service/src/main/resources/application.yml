server:
  port: 8082

spring:
  application:
    name: outing-service
  datasource:
    url: jdbc:mysql://localhost:3306/outing_db?createDatabaseIfNotExist=true
    username: root
    password: shreydave2006
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:                          # ← MUST be under 'spring:', not at root level
    host: smtp.gmail.com
    port: 587
    username: dshrey60@gmail.com
    password: password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/